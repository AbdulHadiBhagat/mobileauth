name: phone_auth
description: A phone authentication tutorial.

version: 1.2.0+3

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: 
  firebase_auth: 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/glogo.png